# Smart Parking Management System - Python dependencies
#
# On a Jetson Orin Nano you should NOT install torch / torchvision via this
# file - they must come from NVIDIA's JetPack-specific wheel. See README.md.
# Install everything else with:  pip install -r requirements.txt

# Web server
flask>=3.0.0
flask-socketio>=5.3.0

# Persistence + config
sqlalchemy>=2.0.0
pyyaml>=6.0

# Computer vision
opencv-python>=4.10.0,<4.11
numpy>=1.26,<2.0
ultralytics>=8.4
easyocr>=1.7

# TensorRT / ONNX export (only used by the one-off engine build)
onnx>=1.12,<2.0
onnxslim>=0.1.71
